doctype html
html
  head
    title= title
    
    link(rel='stylesheet', href='https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css')
    script(src='https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js')
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    .layout-container
      .row
        .col-xs-4
          img.logo(src='/images/icon.png')
        .col-xs-4
          .title Car Car Car
        
      .row
        .col-xs-4
          
          .container
            .row
              .menu-box
                .col-xs-4
                  a.menu(href='/') Home
                  a.menu(href='/users') List Users
                  a.menu(href='/users/add') Add New User
                  a.menu(href='/parts') View Parts
                  a.menu(href='/parts/add') Add Sparepart
            .row
              .menu-box
                .col-xs-4
                  a.menu(href='/cars') View All Cars
                  a.menu(href='/cars/add') Add Car
            
                  if userid
                    a.menu(href='/dashboard') Dashboard
                    a.menu(href='/logout') Logout
                  else
                    a.menu(href='/login') Login
    block content
